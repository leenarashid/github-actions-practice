name: Call Build
on:
  push:
    branches: [main]
jobs:
  build:
    uses: ./.github/workflows/reusable-build3.yml
    with:
      app_name: "my-app"
      environment: "production"
    secrets:
      docker_token: ${{ secrets.DOCKERHUB_TOKEN }}
  print-job:
    runs-on: ubuntu-latest
    needs: build
    steps:
      - run: |
            echo " The result is: ${{ needs.build.outputs.build }}"
