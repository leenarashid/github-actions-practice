name: Call Build
on:
  push:
    branches: [main]
jobs:
  build:
    uses: ./.github/workflows/reusable-build2.yml
    with:
      app_name: "my-app"
      environment: "production"
    secrets:
      docker_token: ${{ secrets.DOCKERHUB_TOKEN }}
